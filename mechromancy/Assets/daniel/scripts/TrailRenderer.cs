using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TrailRenderer : MonoBehaviour
{
    //this script is used for render a parabola for throwing beacon
    //use prefab intialize to determine position, use particle system to render the trail
   
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
